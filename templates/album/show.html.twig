{% extends 'base.html.twig' %}

{% block title %} Album n°{{ album.id }} {% endblock %}

{% block main %}

<h1>Album</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ album.id }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ album.title }}</td>
        </tr>
        <tr>
            <th>Characters</th>
            <td>{{ album.characters|join(', ') }}</td>
        </tr>
        <tr>
            <th>Creator</th>
            <td>{{ album.membre }}</td>
        </tr>
    </tbody>
</table>

<a href="{{ path('album_index') }}">back to list</a>

<a href="{{ path('app_album_edit', {'id': album.id}) }}">edit</a>

{{ include('album/_delete_form.html.twig') }}
{% endblock %}